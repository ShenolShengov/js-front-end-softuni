function solve(input, start, end){
     console.log(input.substring(start, start + end));
}
solve('ASentence', 1, 8);
solve('SkipWord', 4, 7);
solve("This is example to show how should work program", 35, 13);
